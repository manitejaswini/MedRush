import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "MedRush • Select Type",
};

export default function SelectLayout({ children }: { children: React.ReactNode }) {
  return children;
}


